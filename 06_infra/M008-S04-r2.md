# M008-S04 r2: corrección acotada F1/F2/F3

Entrega coder conforme a D042 y al prompt 073_M008-S04_r2. Baseline publicada
`737fe3a4f4b5521491effa196eb053eb99c9c7a8`; se conserva la baseline arquitectónica
de emisión salvo las cuatro fuentes correctivas autorizadas. No es aceptación.

Hallazgos de procedencia: M008-S04-F1 (P1), M008-S04-F2 y M008-S04-F3 (P2),
`05_governance/reviews/m008/M008-S04_r1_review.md`, SHA-256
`33b937d4e6193ff88620bbbb14d2251a43865697cb0a98a032c55270fcce5062`.
Sólo reviewer puede cerrarlos, citando ese informe y la evidencia r2.

## Implementación

F1: `08_pkg/tests/run_checks.py` utiliza un estado persistente compartido por
las cargas runpy y heredado por los hijos. `WALL2WALL_CHILD_STATE` identifica esa
raíz externa; sin él, se usa VERIFICATION_SCRATCH. unknown.json queda enclavado
tras timeout, interrupción o fallo de control del lanzamiento. Guarda intención,
PID, argv, logs y finalización del hijo. La guarda rechaza nuevos hijos y los
hooks Pytest detienen el avance al observar unknown, conservando la comprobación
heredada de skips. El scratch se conserva sin WALL2WALL_RETAIN_SCRATCH; no se
limpia ante unknown o excepción no controlada. Una salida conocida positiva de
un hijo sigue siendo un fallo ordinario y no impide por sí sola otro hijo.

`08_pkg/workflow/stage_checks.py` crea una raíz externa por selector y enlaza su
estado en el marcador pendiente antes de lanzar. Propaga la guarda y la ruta
WALL2WALL_SELECTOR_RESULT. Sólo retira el marcador tras finalización explícita
conocida, código coincidente y ausencia de unknown. La salida positiva del padre
no basta cuando falta esa evidencia o un descendiente es desconocido. `run.py`
permanece intacto y conserva la exclusión al encontrar el selector pendiente.
No se consulta la vida del PID para desbloquear ni se mata ningún proceso.
Recuperación exige diagnóstico y acción manual explícitos. La regresión simula
recuperación sólo después de observar el fin del auxiliar y eligiendo otra raíz,
sin eliminar marcadores antiguos.

F2: `06_infra/m008_integration/fit_counter.py` y run_child mantienen el Python
seleccionado, -I, el hash del helper y la activación previa del contador. Para
archivos, el padre verifica una ruta ordinaria sin enlaces/reparse points y
transmite su SHA-256; el hijo vuelve a comprobarlo y añade exclusivamente el
directorio del script para imports hermanos, restaurando sys.path al salir.
Preserva argumentos Unicode/espacios. Los modos -c/-m conservan el entorno
importable aislado y no incorporan CWD/PYTHONPATH. No se cambia workflow/run.py
ni se añade una dependencia al bundle; el helper instrumentado sigue siendo
externo y explícito, como en D040.

F3: `06_infra/m008_integration/products.py` valida fold_summary antes de comparar:
claves exactas n_folds/weighting/metrics, weighting canónico no ponderado,
rmse/mae/bias/r2 y campos exactos n_defined/mean/std_population. Rechaza bool,
conteos incompatibles, no finitos, desviación negativa y null sin ausencia de
valores definidos. Recalcula media y desviación poblacional desde los folds y
contrasta con atol=rtol=1e-5. Admite R2 parcial o totalmente indefinido cuando los
folds justifican ese estado. Dos resúmenes igualmente incompletos son rechazados
antes de comparar. El productor modeling._summary no se modifica.

Los cuatro encabezados se actualizan conforme al formato canónico AGENTS.md.
No se modifica repair_s04.py, test_s04_repair.py, qualify_s04_repair.py ni su scope.
No se altera API científica, entorno, locks ni implementación de run.py.

## Evidencia independiente y conservación histórica

D040 conserva sus 32 contratos y D041 sus seis contratos de cualificación, los
snapshots de 67 rutas, inventario de 14 rutas, resúmenes originales, logs, recibos,
fases coder/oficial r1, documento M008-S04.md y dictamen needs_work. Su cotejo r1
satisfactorio no cambia el dictamen ni se atribuye a las fuentes corregidas.

La cualificación previa D042 de seis contratos del gate ya estaba consumida:
`06_infra/m008-s04-r2-gate-qualification.json`, SHA-256
`23068033bedb3b50fe297759a3c5ae78b1c10e060b5c909b8fca08ee5c36088b`.
No se repitió ni se editaron sus cuatro identidades cualificadas.

Las regresiones nuevas usan el inventario explícito de 72 rutas, incluido
CONTEXT, sin actualizar inventarios históricos. El resumen
`06_infra/m008-s04-r2-validation.json` es copia exacta de summary.json original
del destino Windows indicado por `local_state/m008-s04-r2-win32.json`.
Distingue before_r1 y after_r2 y conserva las identidades históricas. Ese enlace
no concede aceptación ni traslada los pases D040 a bytes nuevos.

## Regresiones observadas

Una ejecución por perfil, en orden Linux y después Windows nativo, con las mismas
fuentes corregidas. Ambas finalizaron con código cero; doce IDs exactos por
perfil, sin errores, fallos ni skips externos y cero fits. Los negativos de
unknown son fixtures previstas; se espera su terminación autónoma sin kill.

| Perfil | Invocación | Segundos de despacho | Auxiliares directos | Descendientes | Fits |
| --- | --- | ---: | ---: | ---: | ---: |
| linux | `a7cbfc2c0d9a4e6d963fc3bc8653c47d` | 9.149 | 12 | 1 | 0 |
| win32 | `95d5d49dcb5f4bfd972423f3661df902` | 33.73 | 12 | 1 | 0 |

Los doce IDs bajo test_s04_repair.py son:

- test_unknown_blocks_new_children_across_loads
- test_unknown_stops_pytest_followup
- test_unknown_retains_scratch_without_optin
- test_descendant_unknown_preserves_selector_and_lock
- test_known_failure_and_explicit_recovery
- test_instrumented_script_sibling_imports
- test_instrumented_workflow_help
- test_instrumented_command_modes
- test_summary_complete_and_undefined
- test_summary_schema_and_types_rejected
- test_summary_fold_consistency_rejected
- test_equal_incomplete_metrics_rejected

Originales persistentes y separados, referenciados por los punteros ignorados
local_state/m008-s04-r2-linux.json y local_state/m008-s04-r2-win32.json. Cada uno
conserva configuración, snapshot/manifiesto, scratch, logs, JUnit, started.json,
dispatch-result.json y recibo. Las guardas SQLite registran cero entradas fit.
No se borran estos originales ni la evidencia histórica.

SHA-256 del resumen portable: `fd96dfe877b8a14023bd6405b662c665a34bcd9bf6691903af0597839ce697f2`.

## Reservas y continuación

Comandos de regresión consumidos una vez mediante el Python del proyecto Linux:

    python 06_infra/m008_integration/repair_s04.py --regress --profile linux --config local_state/m008-s04-r2-config.json
    python 06_infra/m008_integration/repair_s04.py --regress --profile win32 --config local_state/m008-s04-r2-config.json

Límites D042: 600 s por perfil, 16 auxiliares directos y cuatro descendientes,
30 s por auxiliar con término autónomo previsto hasta 5 s; una cadena wrapper/
worker por perfil y un hilo. Snapshot 64 MiB/256 archivos, scratch 512 MiB,
evidencia/logs 128 MiB por perfil, gates 64 MiB cada uno, total nuevo 2 GiB.
El dispatcher controla tamaños al cerrar; son comprobaciones finales, no picos.
RSS objetivo 1 GiB por proceso, trabajo coder 90 minutos. Picos RSS/scratch y
tokens unknown. No se ejecutan focused adicionales.

Después de completar este documento, sólo queda al coder un cotejo sin suites:

    python 06_infra/m008_integration/repair_s04.py --phase next

Reserva coder 120 s, sin hijos. Su resultado se informa en la respuesta final;
este documento no se modifica después para conservar su hash. Después de coded,
el arquitecto dispone de una reserva official distinta de 120 s mediante
scripts/verify.py M008-S04 --timeout 120. No repetir next ni cualificaciones.
Cualquier fallo o unknown detiene sin retry ni kill y conserva todos los
punteros y originales; requiere diagnóstico y autoridad nueva del arquitecto.

Reviewer debe inspeccionar el delta completo D040/D041 y estas correcciones,
no sólo el changed posterior ni los nombres de pruebas. La veracidad de los
encabezados y las dependencias forman parte de esa revisión. Sólo reviewer
cierra F1/F2/F3. No se fuerza aceptación ni se reescribe historia.

## Límites científicos

Linux es el único checkout propietario; Windows sólo ejecutó snapshot aislado.
Se conservan la copia histórica y los prefijos. S00/S01/S03 permanecen aceptadas,
S02 pendiente, M006 cerrado y su full S03 r1 unknown. D037 fallido y D038
independiente no se alteran; M007 permanece pendiente y bufa excluido.

Las 24 regresiones r2 no acreditan distribución ejecutada, regresiones científicas
ni equivalencia Windows/Linux. No se ejecutaron builds, production/validated,
réplicas, canaries ni fits. Las 368 entradas fit siguen sin autorización.
S02/M008 no se cierran y atol=rtol=1e-5 permanece vigente. No commit ni push.
