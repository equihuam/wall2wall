# M008-S02: ejecución científica conservada

Entrega coder conforme al contrato 075_M008-S02_r1 y a la autorización posterior
D044. Baseline publicada `d665129e70dca0b0dfd5edf90a02f78e796cad81` y delta
arquitectónico D043/D044 conservados sin nueva implementación. Este documento
no concede aceptación de S02 ni cierra M008.

## Autoridad y conservación

La autoridad humana `00_brief/M008-S02-science-authority.json` admite un único
despacho secuencial Linux/Windows, máximo 368 entradas fit, sin nuevos builds.
Está ligada al inventario ordenado de 79 fuentes, SHA-256 canónico
`eeb58c2117ce7148450be2e5e75dc281265b814a7835cf7bb0e65e5597268be5`.
No modifica las reservas consumidas de preparación.

D043 conserva sus 28 contratos, cuatro originales y cotejo; informe SHA-256
`71cc232e7addc3b6c2fe1d38fcaf969d4baacce9ad4d4991df2880429a7226fe`.
D044 conserva sus doce contratos y cotejo, cero fits/builds; informe SHA-256
`fa1a7cda8f05dd5176f5235440dc74e48cf68571501c1b17244f2c8ce7772556`.
Su primer intento Linux fallido permanece consumido; diagnóstico y versiones
previas siguen enlazados por los punteros ignorados de fallo y recuperación.
El puente `06_infra/m008-s02-d043-bridge.json` mantiene las 76 identidades
históricas y las cuatro diferencias auxiliares; no traslada pases D043 a bytes
posteriores. Las preparaciones con dobles se distinguen de esta ejecución real.

Linux conserva fuentes y gobernanza. Windows sólo recibió una instantánea nueva
y destinos externos persistentes. No se editaron fuentes, gates, puente, informes
históricos, locks, prefijos ni la copia Windows histórica. No se repitieron
cualificaciones, preparaciones, suites históricas o builds.

## Recorrido autorizado

Se ejecutó una sola vez mediante el Python del proyecto Linux:

    python 06_infra/m008_integration/science_dispatch.py --science --config local_state/m008-s02-science-config.json

El dispatcher controló las fases subordinadas; no se lanzaron aparte selectores
ni comandos científicos. Antes del primer fit materializó ambas instantáneas,
raíces separadas y la fixture común: copias byte-idénticas de la fixture Linux
conservada. Los originales Linux LF y Windows CRLF permanecen intactos. Las
configuraciones nuevas adaptan rutas, perfil y locks; los parámetros científicos
y entradas quedan enlazados mediante provenance y fixture/origin.json.

Se reutilizaron las distribuciones conservadas sólo tras cotejar inputs, wheel,
instalación externa y bundle. No hubo rebuild ni instalación nueva. El DAG de
usuario utiliza el producto instalado desde wheel mediante origen explícito;
las pruebas que insertan src siguen siendo regresiones de fuentes, no se atribuye
a todas importación desde wheel.

Cada perfil realizó workflow, una production desde distribución, validated sobre
esa misma producción y los dos no-op. La matriz workflow incluye invalidación,
reparación y reinicio sin repetir escenarios fuera de sus catorce IDs. Los no-op
conservaron las identidades y mtimes exigidos dentro de cada perfil.

## Resultado científico observado

El despacho global terminó con código cero. Los informes por perfil registran
status=passed, unknown=false y Python 3.11.16 en sus plataformas respectivas.

| Perfil | Segundos de despacho | IDs workflow | IDs validated | Entradas fit |
| --- | ---: | ---: | ---: | ---: |
| linux | 268.414 | 14 | 187 | 184 |
| win32 | 533.985 | 14 | 187 | 184 |

Por perfil: workflow 37, production 5, validated 142, production-noop 0 y
validated-noop 0. Total observado y reservado: 184 por perfil, 368 ambos.
Los conjuntos exactos son los de M008-S02-planned-ids-d043.json, sin IDs extra,
ausentes, skips ni fallos externos de suite. Los dos intentos fit fallidos
registrados en validated por perfil pertenecen a negativos previstos y cuentan
dentro del presupuesto; no son fallos externos de la fase, que salió con cero.

SQLite conserva reservas, proceso, clase, prueba y estado por entrada pública.
Incluye Pipeline y pasos, sin sumar árboles internos RF ni duplicar wrappers.
La propuesta histórica 364 no se reescribe: D040 fijó 368 al contar las dos
entradas Pipeline adicionales de modeling, 47 frente a su contador local 45.
Los conteos no se reajustaron tras observar resultados.

La única comparación de los productos reales devolvió equal=true,
atol=rtol=1e-5. Lee CSV/JSON/GeoTIFF y compara claves/tipos, OOF, folds,
exclusiones, métricas, mapas, CRS, grilla, máscaras y flags conforme a products.py.
NaN/None sólo se admiten donde el contrato lo permite; no compara joblib binario.
comparison.json enlaza los hashes de productos de ambos perfiles y la misma
identidad de fixture. El resultado se limita a estos fixtures y perfiles fijos.

Invocación global: `136f38edb1a845388cece21796a7e9ad`.
Resumen portable SHA-256: `7354f6f488dd6abbc0523e8299fee3e37299a6cda7b58840226fb8bb617204cd`.
Comparación SHA-256: `3302c1b793c2d1de80cc9ae8a1301e933c34a7cf99019c5629541d6df98212b3`.

El dispatcher creó `06_infra/m008-s02-validation.json`; sus bytes coinciden con
summary.json externo. El puntero ignorado
`local_state/m008-s02-science-attempt.json` enlaza la raíz global. Sus punteros
linux-pointer.json y win32-pointer.json enlazan cada perfil. Se conservan
snapshots/manifiestos, configuración, procedencia, reserva, PIDs/argv, logs,
finalizaciones, JUnit, SQLite, recibos, productos y comparación. No se incorporan
rutas resueltas de máquina al documento ni se borran originales.

## Límites y cotejos pendientes

Reservas consumidas por perfil: workflow 1/1200 s/37 fits, production 1/1200 s/5,
validated 1/1200 s/142; no-op production y validated 1/120 s/0 cada uno.
Despacho 3960 s por perfil, global 9000 s, comparación única 120 s.
Almacenamiento: snapshot 64 MiB/256 archivos, scratch 512 MiB por comando,
logs/evidencia 128 MiB por perfil, retenido 2 GiB por perfil y 5 GiB global.
Perfiles secuenciales y un hilo por biblioteca. El dispatcher comprobó los límites
finales; no son mediciones de picos. RSS objetivo 1 GiB; RSS/scratch máximos y
tokens unknown si no medidos. Fallo o unknown consume y detiene sin retry ni kill.

Tras completar este documento corresponde únicamente el cotejo coder reservado:

    python 06_infra/m008_integration/verify_s02.py --phase next

Una invocación, 120 s, cero fits ejecutados; sólo coteja originales, fases,
no-op, marcadores, configuración, fixture, SQLite, JUnit, productos y headers
sobre el scope explícito. No reejecuta suites, comparación ni builds. El resultado
coder se informa en la respuesta final. No volver a invocar next bajo esta entrega.
Después de coded ordinario, el arquitecto dispone del cotejo oficial distinto
scripts/verify.py M008-S02 --timeout 120, seguido de revisión ordinaria. Reviewer
inspecciona todo el delta D043/D044 contra la baseline y las adiciones, además
de la evidencia científica; no sólo changed posterior. No se presume su dictamen.

S00/S01/S03/S04 y el cierre por revisión de F1/F2/F3 se conservan. M006 permanece
cerrado, su full S03 r1 unknown sigue consumido; D037 fallido, D038 independiente
y D036 histórico permanecen intactos. M007 sigue pendiente y bufa excluido.
No datos reales, motores extra, réplica, canary, cambios de algoritmos/API ni
entornos nuevos. No se generaliza equivalencia a otras versiones, fixtures o
plataformas ni se cierra M008. No commit ni push.
